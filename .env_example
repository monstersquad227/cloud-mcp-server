ALIYUN_ACCESS_KEY_ID=
ALIYUN_ACCESS_KEY_SECRET=
ALIYUN_REGION_ID=cn-shanghai
# kubeconfig 所在目录，默认项目根目录
# 约定: 不传 cluster -> kubeconfig；fat -> fatkubeconfig；prod -> prodkubeconfig
KUBECONFIG_DIR=
# 全局 SSL 校验；也可按环境覆盖，如 KUBERNETES_VERIFY_SSL_FAT=false
KUBERNETES_VERIFY_SSL=true
